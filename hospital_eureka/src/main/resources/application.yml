server:
  port: 8761

spring:
  application:
    name: eureka-service

eureka:
  client:
    service-url:
      defaultZone: http://127.0.0.1:8761/eureka
    fetch-registry: false #不从其他eureka中提供服务列表
    register-with-eureka: false #不从其他eureka中注册服务